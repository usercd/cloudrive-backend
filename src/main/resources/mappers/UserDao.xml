<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloudrive.dao.UserDao">
    <select id="findUserByEmail" resultType="com.cloudrive.model.entity.User">
        SELECT user_id,
               avatar_url,
               username,
               password,
               email,
               status,
               create_time,
               update_time
        FROM t_user
        WHERE email = #{email}
    </select>

    <select id="findUserById" resultType="com.cloudrive.model.entity.User">
        SELECT user_id,
               avatar_url,
               username,
               password,
               email,
               status,
               create_time,
               update_time
        FROM t_user
        WHERE user_id = #{userId}
    </select>

    <insert id="save">
        INSERT INTO t_user (user_id, username, password, email, status)
        VALUES (#{userId}, #{username}, #{password}, #{email}, #{status})
    </insert>
</mapper>